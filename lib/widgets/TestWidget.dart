import 'package:flutter/material.dart';

class Testwidget extends StatelessWidget{
  @override 
  Widget build(BuildContext context){
    return Center(
      child: Container(
        alignment: Alignment.center,
        child: Text('This is a text widgets'),
      )
    );
  }
}